# 第 19 章：Docker 镜像与 Kubernetes

在前面的章节中，我们经常看到这样的 Kubernetes 配置：

```
containers:
  - name: api
    image: nginx:1.27
```

这里的：

```
nginx:1.27
```

就是一个 **Container Image（容器镜像）**。

Kubernetes 本身并不负责构建镜像，它主要负责：

```
获取镜像
   ↓
启动 Container
   ↓
管理 Pod
   ↓
根据声明维持应用运行
```

所以要把 Kubernetes 应用真正部署到生产环境，必须理解：

```
代码
 ↓
Docker Image
 ↓
Registry
 ↓
Kubernetes Node
 ↓
Container
 ↓
Pod
```

------

## 19.1 Image 基础

### 19.1.1 Image 是什么

Docker Image（容器镜像）可以理解成：

> **运行一个 Container 所需要的软件、依赖、文件系统和元数据的只读模板。**

例如一个 .NET API：

```
.NET API Image
├── Linux 基础文件
├── .NET Runtime
├── Application DLL
├── appsettings.json
└── 其他依赖
```

启动后：

```
Image
  ↓
Container
```

可以把它简单理解成：

```
Image = 安装好的应用模板
Container = Image 的运行实例
```

------

### 19.1.2 Image 与 Container 的关系

例如：

```
nginx:1.27
```

是一份 Image。

可以启动多个 Container：

```
nginx:1.27
    │
    ├── Container A
    ├── Container B
    └── Container C
```

Kubernetes 中：

```
Deployment
    ↓
Pod
    ↓
Container
    ↓
Image
```

例如：

```
containers:
  - name: api
    image: nginx:1.27
```

Kubernetes 会让 Node 上的 Container Runtime 获取：

```
nginx:1.27
```

然后启动 Container。

------

### 19.1.3 Image 通常包含什么

一个镜像通常包含：

```
Application
Runtime
Libraries
System utilities
Configuration defaults
Metadata
```

例如 Node.js 应用：

```
Node.js Image
├── Linux filesystem
├── Node.js runtime
├── npm
├── Application source/build output
└── dependencies
```

而对于生产环境，通常不会直接把开发环境全部塞进去。

例如：

```
TypeScript
 ↓
npm install
 ↓
npm run build
 ↓
dist/
```

最终运行镜像可能只需要：

```
Node.js runtime
+
dist/
+
production dependencies
```

这就是后面多阶段构建和镜像优化的基础。

------

## 19.2 Registry

### 19.2.1 Registry 是什么

Registry 是：

> **存放和分发 Container Image 的镜像仓库。**

可以理解成：

```
Git Repository
    ↓
保存源代码

Container Registry
    ↓
保存 Container Image
```

例如：

```
Developer
   ↓
docker build
   ↓
Image
   ↓
docker push
   ↓
Registry
```

Kubernetes：

```
Registry
   ↓
Node
   ↓
Image
   ↓
Container
```

------

### 19.2.2 为什么 Kubernetes 需要 Registry

假设你有：

```
Node-1
Node-2
Node-3
```

而应用镜像是：

```
my-api:1.0.0
```

如果镜像只存在开发人员电脑：

```
Developer PC
└── my-api:1.0.0
```

Kubernetes Node 无法直接使用。

所以需要：

```
Developer
    ↓
Registry
    ↓
┌───────┬───────┬───────┐
Node-1 Node-2 Node-3
```

每个 Node 都可以从 Registry 拉取镜像。

------

### 19.2.3 Push 与 Pull

开发人员：

```
docker push registry.example.com/my-api:1.0.0
```

意味着：

```
本地 Image
    ↓
Registry
```

Kubernetes：

```
registry.example.com/my-api:1.0.0
```

意味着 Node 需要：

```
Registry
    ↓
Node
```

也就是 Pull。

------

## 19.3 Docker Hub

### 19.3.1 Docker Hub 是什么

Docker 提供的 Docker Hub 是非常常见的公共 Container Registry。

它可以存储：

```
Public Images
Private Images
```

例如：

```
nginx
redis
postgres
```

等大量公共镜像。

------

### 19.3.2 Kubernetes 使用 Docker Hub

例如：

```
containers:
  - name: nginx
    image: nginx:1.27
```

如果没有指定 Registry：

```
nginx:1.27
```

通常表示：

```
docker.io/library/nginx:1.27
```

Kubernetes Node 会尝试从 Docker Hub 拉取。

------

### 19.3.3 Docker Hub 适合什么场景

非常适合：

```
学习
测试
个人项目
公共基础镜像
```

例如：

```
image: nginx:1.27
```

学习 Kubernetes 时非常方便。

但是生产环境通常不会简单依赖：

```
公网 Docker Hub
```

而会考虑：

```
企业私有 Registry
镜像缓存
镜像代理
Harbor
云厂商 Registry
```

------

## 19.4 Harbor

### 19.4.1 Harbor 是什么

Harbor 是一个开源的企业级 Container Registry。

它可以部署在企业内部：

```
Developer
   │
   ▼
 Harbor
   │
   ├── Project A
   ├── Project B
   └── Project C
```

相比简单的 Registry，Harbor 通常提供更加完整的企业能力，例如：

```
用户权限
Project
镜像管理
镜像扫描
Replication
审计
访问控制
```

------

### 19.4.2 为什么生产环境常见 Harbor

假设企业有：

```
10 个团队
100 个服务
1000 个镜像版本
```

如果全部依赖：

```
Docker Hub
```

会产生：

```
权限管理
镜像安全
网络访问
审计
镜像生命周期
```

等问题。

使用 Harbor：

```
开发环境
       │
       ▼
    Harbor
       │
 ┌─────┼─────┐
 ▼     ▼     ▼
K8S-1 K8S-2 K8S-3
```

可以把企业镜像集中管理。

------

### 19.4.3 Harbor 项目

例如：

```
Harbor
└── Project: production
    ├── order-api
    ├── user-api
    └── payment-api
```

镜像可能是：

```
harbor.example.com/production/order-api:1.0.0
```

Kubernetes：

```
containers:
  - name: order-api
    image: harbor.example.com/production/order-api:1.0.0
```

------

## 19.5 私有 Registry

### 19.5.1 为什么需要私有 Registry

生产环境通常需要：

```
企业内部镜像
```

例如：

```
registry.example.com/backend/order-api:1.0.0
```

而不是：

```
docker.io/andy/order-api:1.0.0
```

主要原因包括：

```
安全
权限
网络
合规
镜像访问速度
供应链控制
```

------

### 19.5.2 私有 Registry 的基本架构

```
CI/CD
  │
  │ docker push
  ▼
Private Registry
  │
  │ image pull
  ▼
Kubernetes
  │
  ├── Node-1
  ├── Node-2
  └── Node-3
```

------

### 19.5.3 私有 Registry 必须考虑 HTTPS

生产环境不要简单使用：

```
http://registry.example.com
```

而应该：

```
https://registry.example.com
```

原因是镜像本身可能包含：

```
业务代码
依赖
配置
内部组件
```

并且 Registry 的认证信息也需要安全传输。

------

## 19.6 ImagePullSecrets

### 19.6.1 为什么需要 ImagePullSecrets

如果 Registry 是私有的：

```
harbor.example.com
```

Kubernetes Node：

```
docker pull
```

需要认证。

例如：

```
Username
Password
```

Kubernetes 可以使用：

```
imagePullSecrets
```

保存 Registry 认证信息。

------

### 19.6.2 创建 Registry Secret

例如：

```
kubectl create secret docker-registry harbor-secret \
  --docker-server=harbor.example.com \
  --docker-username=admin \
  --docker-password='YOUR_PASSWORD'
```

查看：

```
kubectl get secret harbor-secret
```

注意：

> Secret 本身并不会神奇地让密码变成不可读取的密文。

这一点后面会继续涉及。

------

### 19.6.3 Pod 使用 ImagePullSecrets

```
apiVersion: v1
kind: Pod
metadata:
  name: api

spec:
  imagePullSecrets:
    - name: harbor-secret

  containers:
    - name: api
      image: harbor.example.com/production/api:1.0.0
```

如果使用 Deployment：

```
spec:
  template:
    spec:
      imagePullSecrets:
        - name: harbor-secret

      containers:
        - name: api
          image: harbor.example.com/production/api:1.0.0
```

------

### 19.6.4 推荐绑定到 ServiceAccount

如果一个 Namespace 中大量 Pod 都需要访问同一个 Registry，可以考虑：

```
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-sa
imagePullSecrets:
  - name: harbor-secret
```

然后：

```
spec:
  serviceAccountName: app-sa
```

这样可以减少每个 Deployment 重复配置。

------

## 19.7 Image Tag

### 19.7.1 Tag 是什么

例如：

```
nginx:1.27
```

这里：

```
nginx
```

是 Repository。

```
1.27
```

是 Tag。

完整形式：

```
registry/repository:tag
```

例如：

```
harbor.example.com/production/order-api:1.2.3
```

------

### 19.7.2 Tag 的作用

Tag 通常用于标记：

```
版本
环境
发布批次
```

例如：

```
order-api:1.0.0
order-api:1.1.0
order-api:2.0.0
```

也可以：

```
order-api:production
order-api:staging
```

但生产环境更推荐使用明确的不可变版本标识。

------

## 19.8 `latest` 的问题

### 19.8.1 `latest` 是什么

例如：

```
image: my-api:latest
```

它只是一个 Tag。

并不代表：

```
最新代码
```

也不代表：

```
生产环境最新稳定版本
```

它只是：

```
一个叫 latest 的标签
```

------

### 19.8.2 为什么生产环境不推荐 latest

假设今天：

```
latest → Image A
```

Deployment：

```
image: my-api:latest
```

明天：

```
latest → Image B
```

但是 Kubernetes YAML 完全没变化。

这会造成：

```
昨天部署：
latest = A

今天部署：
latest = B
```

从 YAML 很难判断实际运行的到底是哪一个 Image。

------

### 19.8.3 回滚困难

假设：

```
latest → v2
```

v2 有 Bug。

你想回滚：

```
v1
```

但是：

```
latest
```

这个名字本身没有表达版本。

因此生产环境推荐：

```
image: harbor.example.com/production/order-api:1.8.3
```

而不是：

```
image: harbor.example.com/production/order-api:latest
```

------

## 19.9 Image Digest

### 19.9.1 Digest 是什么

Image Tag：

```
my-api:1.0.0
```

实际上最终对应一个镜像内容摘要：

```
sha256:xxxxxxxxxxxxxxxx...
```

例如：

```
my-api@sha256:abcdef123456...
```

Digest 可以理解为：

> **镜像内容的唯一标识。**

------

### 19.9.2 Tag 与 Digest 的区别

Tag：

```
my-api:1.0.0
```

是人为定义的名字。

Digest：

```
my-api@sha256:abc...
```

对应具体的镜像内容。

因此：

```
Tag → 可以被重新指向
Digest → 对应确定内容
```

例如：

```
1.0.0 → Image A
```

理论上 Registry 管理者可以重新修改：

```
1.0.0 → Image B
```

但是：

```
sha256:AAA
```

仍然对应原来的 Image A。

------

### 19.9.3 生产环境为什么重视 Digest

如果要求：

```
今天部署的镜像
=
明天部署的镜像
=
任何 Node 拉取的镜像
```

那么 Digest 非常有价值。

例如：

```
image: harbor.example.com/production/order-api@sha256:abcdef...
```

这样可以实现更加严格的：

```
Image Immutability
```

和部署可追踪性。

------

## 19.10 镜像版本管理

### 19.10.1 推荐使用明确版本

例如：

```
order-api:1.0.0
order-api:1.0.1
order-api:1.1.0
order-api:2.0.0
```

不要：

```
order-api:latest
```

------

### 19.10.2 不推荐覆盖同一个生产 Tag

例如：

```
order-api:prod
```

今天：

```
prod → v1
```

明天：

```
prod → v2
```

这会降低部署可追踪性。

更好的方式：

```
order-api:1.0.0
order-api:1.1.0
```

Deployment：

```
image: harbor.example.com/production/order-api:1.1.0
```

------

### 19.10.3 CI/CD 推荐版本策略

例如 Git：

```
Commit
 ↓
CI Build
 ↓
Image
```

可以使用：

```
order-api:<git-sha>
```

例如：

```
order-api:8f3a9d2
```

也可以结合正式版本：

```
order-api:1.4.2
```

生产环境常见做法：

```
Git Commit
     ↓
Build
     ↓
Image
     ↓
Scan
     ↓
Push Registry
     ↓
Deploy
```

这样可以追溯：

```
Pod
 ↓
Image
 ↓
Image Digest
 ↓
Git Commit
```

------

## 19.11 多阶段构建

### 19.11.1 为什么需要多阶段构建

假设一个 .NET 应用：

```
Source Code
 ↓
dotnet restore
 ↓
dotnet build
 ↓
dotnet publish
```

构建过程需要：

```
SDK
Compiler
NuGet
Build tools
```

但是运行应用可能只需要：

```
.NET Runtime
```

如果全部放进最终镜像：

```
Image
├── SDK
├── Compiler
├── Build tools
├── Source
└── Application
```

镜像会非常大。

------

### 19.11.2 Multi-stage Build

Docker 支持：

```
Build Stage
       ↓
Runtime Stage
```

例如 .NET：

```
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build

WORKDIR /src

COPY . .

RUN dotnet restore

RUN dotnet publish \
    -c Release \
    -o /app/publish \
    --no-restore


FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime

WORKDIR /app

COPY --from=build /app/publish .

ENTRYPOINT ["dotnet", "MyApi.dll"]
```

最终：

```
Build Image
├── SDK
├── Source
└── Compiler

        ↓ COPY --from

Runtime Image
├── Runtime
└── Published Application
```

最终镜像不需要包含 SDK。

------

### 19.11.3 Node.js 示例

```
FROM node:22 AS build

WORKDIR /app

COPY package*.json ./

RUN npm ci

COPY . .

RUN npm run build


FROM node:22-slim AS runtime

WORKDIR /app

COPY --from=build /app/dist ./dist
COPY --from=build /app/node_modules ./node_modules
COPY package*.json ./

CMD ["node", "dist/server.js"]
```

核心思想仍然是：

```
Build Environment
       ↓
编译
       ↓
Runtime Environment
```

------

## 19.12 镜像大小优化

镜像越大，通常意味着：

```
Pull 时间更长
网络流量更多
Node 磁盘占用更多
Pod 启动更慢
```

因此生产环境应该合理控制镜像大小。

------

### 19.12.1 选择合适的 Base Image

例如：

```
FROM node:22
```

通常比：

```
FROM node:22-slim
```

更大。

可以根据应用需求选择：

```
完整镜像
Slim
Alpine
Distroless
```

但不能机械地认为：

> Alpine 一定最好。

因为某些应用依赖：

```
glibc
native libraries
```

使用 Alpine 的 musl libc 可能产生兼容性问题。

所以应该：

> **以应用兼容性和安全性为前提进行优化。**

------

### 19.12.2 `.dockerignore`

如果项目：

```
node_modules/
.git/
dist/
logs/
```

不需要进入 Build Context，可以：

```
.dockerignore
```

例如：

```
node_modules
.git
dist
coverage
*.log
.env
```

然后：

```
COPY . .
```

就不会把这些文件发送给 Docker Build Context。

------

### 19.12.3 删除无用文件

例如不要在生产镜像里保留：

```
测试文件
Git 历史
文档
编译缓存
开发依赖
日志
临时文件
```

------

### 19.12.4 不要把 Secret 打进 Image

非常重要。

错误做法：

```
ENV DB_PASSWORD=123456
```

或者：

```
COPY .env .
```

这样 Secret 可能进入 Image Layer。

即使之后：

```
RUN rm .env
```

也不一定真正从历史 Layer 中消失。

正确思路是：

```
Image
 ↓
不包含 Secret
 ↓
Kubernetes Secret
 ↓
Pod
```

也就是：

```
应用镜像
+
运行时配置
=
Container
```

而不是：

```
Secret
+
Image
=
新 Image
```

------

## 19.13 K8S 拉取镜像失败排查

这是生产环境非常常见的问题。

例如：

```
kubectl get pods
```

发现：

```
NAME                       STATUS
order-api-xxx              ImagePullBackOff
```

或者：

```
ErrImagePull
```

------

### 19.13.1 第一步：查看 Pod

```
kubectl get pod
```

例如：

```
NAME              READY   STATUS             RESTARTS
order-api-xxx     0/1     ImagePullBackOff    0
```

------

### 19.13.2 第二步：查看 Pod Events

最重要的命令之一：

```
kubectl describe pod order-api-xxx
```

重点查看：

```
Events:
```

例如：

```
Failed to pull image
```

------

### 19.13.3 常见错误一：镜像不存在

例如：

```
manifest unknown
```

通常意味着：

```
Repository 不存在
```

或者：

```
Tag 不存在
```

检查：

```
image: harbor.example.com/production/order-api:1.0.0
```

确认 Registry 中是否真的存在：

```
order-api
1.0.0
```

------

### 19.13.4 常见错误二：认证失败

例如：

```
unauthorized
authentication required
```

说明 Node 没有正确认证 Registry。

检查：

```
kubectl get secret
```

然后：

```
kubectl describe pod <pod-name>
```

确认：

```
imagePullSecrets:
  - name: harbor-secret
```

是否正确。

------

### 19.13.5 常见错误三：Secret 不存在

例如：

```
secret "harbor-secret" not found
```

检查：

```
kubectl get secret harbor-secret
```

如果不存在：

```
kubectl create secret docker-registry harbor-secret \
  --docker-server=harbor.example.com \
  --docker-username=YOUR_USERNAME \
  --docker-password='YOUR_PASSWORD'
```

------

### 19.13.6 常见错误四：Registry 网络无法访问

例如：

```
dial tcp ... timeout
```

可能是：

```
Node
 ↓
Registry
```

之间网络不通。

检查：

```
kubectl get nodes -o wide
```

然后从对应 Node 的运行环境检查 Registry：

```
curl -vk https://harbor.example.com/v2/
```

如果需要认证，返回：

```
401 Unauthorized
```

反而可能说明：

```
Registry 网络已经可以访问
```

因为 Registry 已经响应请求，只是需要认证。

------

### 19.13.7 常见错误五：TLS Certificate 问题

例如：

```
x509: certificate signed by unknown authority
```

通常意味着：

```
Node / Container Runtime
```

不信任 Registry 使用的 CA。

例如企业 Harbor 使用：

```
内部 CA
```

那么每个 Kubernetes Node 的 Container Runtime 都需要正确配置 CA 信任。

这是生产私有 Registry 非常重要的一环。

------

### 19.13.8 常见错误六：HTTP Registry

例如 Registry：

```
http://harbor.example.com
```

而 Container Runtime 默认要求 HTTPS。

可能出现：

```
http: server gave HTTP response to HTTPS client
```

生产环境推荐直接解决：

```
HTTPS
+
正确 CA
```

而不是简单把 Registry 配置成 insecure registry。

------

### 19.13.9 常见错误七：镜像架构不匹配

例如你的镜像只构建了：

```
linux/amd64
```

但是 Kubernetes Node 是：

```
linux/arm64
```

可能出现：

```
no matching manifest
```

检查 Node：

```
kubectl get nodes -o wide
```

更准确可以：

```
kubectl get nodes \
  -o custom-columns=NAME:.metadata.name,ARCH:.status.nodeInfo.architecture
```

例如：

```
NAME       ARCH
node-1     amd64
node-2     arm64
```

如果集群是混合架构，镜像最好构建多架构版本。

例如：

```
docker buildx build \
  --platform linux/amd64,linux/arm64 \
  -t harbor.example.com/production/order-api:1.0.0 \
  --push .
```

这样 Registry 中可以包含：

```
linux/amd64
linux/arm64
```

Kubernetes Node 会选择匹配自己架构的镜像。

------

### 19.13.10 常见错误八：磁盘空间不足

Node 如果磁盘快满：

```
No space left on device
```

可能无法继续拉取镜像。

查看：

```
df -h
```

以及 Kubernetes：

```
kubectl describe node <node-name>
```

检查：

```
DiskPressure
```

例如：

```
Conditions:
  DiskPressure   True
```

这说明 Node 存储压力已经比较严重。

生产环境需要关注：

```
Container Image
Container Logs
containerd
/var/lib/containerd
/var/lib/kubelet
```

等空间使用情况。

------

### 19.13.11 常见错误九：镜像拉取慢

例如：

```
Pod 长时间 Pending
```

然后 Events：

```
Pulling image
```

可能是：

```
Registry 距离 Node 太远
公网网络慢
镜像太大
Registry 性能不足
```

解决思路：

```
减小 Image
+
企业内部 Registry
+
Registry Mirror / Cache
+
合理的 Registry 网络架构
```

------

### 19.13.12 一套完整排查流程

遇到：

```
ImagePullBackOff
```

不要直接猜。

按照下面顺序：

```
kubectl get pod <pod-name>
```

然后：

```
kubectl describe pod <pod-name>
```

重点：

```
Events
```

然后确认 Image：

```
kubectl get pod <pod-name> \
  -o jsonpath='{.spec.containers[*].image}'
```

检查 Secret：

```
kubectl get secret
```

检查 Pod：

```
kubectl get pod <pod-name> -o yaml
```

确认：

```
image:
imagePullSecrets:
```

再检查 Node：

```
kubectl get pod <pod-name> -o wide
```

找到 Pod 所在 Node 后：

```
kubectl describe node <node-name>
```

重点检查：

```
DiskPressure
Architecture
Conditions
```

最终把问题归类：

```
ImagePullBackOff
       │
       ├── Image 不存在
       ├── Tag 不存在
       ├── Registry 认证失败
       ├── imagePullSecrets 错误
       ├── DNS 问题
       ├── 网络问题
       ├── TLS / CA 问题
       ├── 镜像架构问题
       ├── Registry 权限问题
       └── Node 磁盘问题
```

------

## 本章核心知识

这一章最重要的是建立完整的 **Image 生命周期**：

```
源代码
  │
  ▼
Dockerfile
  │
  ▼
docker build
  │
  ▼
Image
  │
  │ docker push
  ▼
Registry
  │
  │ image pull
  ▼
Kubernetes Node
  │
  ▼
Container
  │
  ▼
Pod
```

生产环境推荐的基本原则可以浓缩成：

```
① 不使用 latest 作为生产版本
② 使用明确、可追踪的 Image Tag
③ 更严格的场景使用 Image Digest
④ 私有镜像使用 ImagePullSecrets
⑤ 生产环境使用企业级 Registry
⑥ 镜像尽量小
⑦ 使用 Multi-stage Build
⑧ 不把 Secret 打进 Image
⑨ 构建并验证正确的 CPU Architecture
⑩ ImagePullBackOff 优先看 kubectl describe pod 的 Events
```

尤其要记住一个生产环境非常重要的关系：

```
Tag
 ↓
Digest
 ↓
具体 Image 内容
```

以及：

```
Kubernetes YAML
        ↓
image: registry/app:1.2.3
        ↓
Registry
        ↓
具体 Image
        ↓
Container
```

**镜像不是 Kubernetes 创建出来的，Kubernetes 的职责是使用和管理镜像运行出来的 Container。**

# 第 20 章：从代码到 Kubernetes

这一章我们把前面已经学习过的内容真正串起来。

目标不是单独部署一个 Pod，而是完整走通一条生产环境中非常典型的业务发布链路：

```
源代码
 ↓
编译
 ↓
Docker Image
 ↓
Registry
 ↓
Deployment
 ↓
Pod
 ↓
Service
 ↓
Ingress
 ↓
用户访问
```

最终我们构建这样一个业务系统：

```
                         Internet
                            │
                            ▼
                    ┌──────────────┐
                    │    Ingress   │
                    │ HTTPS / 域名 │
                    └──────┬───────┘
                           │
                 ┌─────────┴─────────┐
                 ▼                   ▼
          ┌─────────────┐     ┌─────────────┐
          │ Vue Frontend│     │   .NET API  │
          │    Nginx    │     │ Deployment  │
          └─────────────┘     └──────┬──────┘
                                     │
                              ┌──────┴──────┐
                              ▼             ▼
                           Redis       PostgreSQL
```

同时：

```
ConfigMap
    ↓
应用配置

Secret
    ↓
数据库密码 / Redis 密码

TLS Secret
    ↓
HTTPS
```

这一章会特别注意一个问题：

> **开发环境能运行，不等于 Kubernetes 生产环境能稳定运行。**

所以我们不仅关注“怎么部署”，还会关注镜像、配置、Secret、健康检查、持久化、Service、Ingress，以及最终如何验证整个系统。

------

### 实战系统架构

我们先明确最终要运行什么。

假设业务系统叫：

```
shop
```

包含：

```
frontend
    Vue 3
    ↓
    Nginx

backend
    ASP.NET Core API
    ↓
    Redis
    ↓
    PostgreSQL
```

最终 Kubernetes 中大致是：

```
Namespace: shop

frontend
├── Deployment
├── Pod
└── Service

backend
├── Deployment
├── Pod
└── Service

redis
├── StatefulSet
├── Pod
├── Service
└── PVC

postgresql
├── StatefulSet
├── Pod
├── Service
└── PVC

Ingress
└── shop.example.com
```

配置：

```
ConfigMap
├── API 地址
├── Redis 地址
└── 数据库地址

Secret
├── PostgreSQL Password
├── Redis Password
└── 其他敏感配置

TLS Secret
└── HTTPS Certificate
```

------

### 项目目录

为了让整个实践可以真正落地，可以准备如下目录：

```
shop/
├── backend/
│   ├── Shop.Api/
│   ├── Shop.Api.csproj
│   └── Dockerfile
│
├── frontend/
│   ├── src/
│   ├── package.json
│   ├── nginx.conf
│   └── Dockerfile
│
└── k8s/
    ├── namespace.yaml
    ├── configmap.yaml
    ├── secret.yaml
    ├── redis.yaml
    ├── postgresql.yaml
    ├── backend.yaml
    ├── frontend.yaml
    └── ingress.yaml
```

实际生产项目通常会进一步拆分，例如：

```
k8s/
├── base/
└── overlays/
    ├── dev/
    ├── staging/
    └── production/
```

这里先保持结构简单，重点是理解完整发布过程。

------

## 源代码

### .NET API

假设后端是 ASP.NET Core Web API。

一个最简单的 API：

```
var builder = WebApplication.CreateBuilder(args);

builder.Services.AddControllers();

var app = builder.Build();

app.MapGet("/api/health", () => Results.Ok(new
{
    status = "ok"
}));

app.MapGet("/api/hello", () => Results.Ok(new
{
    message = "Hello Kubernetes"
}));

app.Run();
```

启动：

```
dotnet run
```

测试：

```
curl http://localhost:5000/api/health
```

得到：

```
{
  "status": "ok"
}
```

这里有一个非常重要的生产思维：

> **在进入 Docker 和 Kubernetes 之前，首先保证应用本身可以正常运行。**

否则后面出现问题时，很难判断到底是：

```
代码问题
Docker 问题
Kubernetes 问题
```

------

### Vue 前端

Vue 应用最终通常需要：

```
Vue Source
   ↓
npm install
   ↓
npm run build
   ↓
dist/
```

例如：

```
npm install
npm run build
```

得到：

```
dist/
├── index.html
├── assets/
└── ...
```

Vue 生产环境通常不需要 Node.js 一直运行。

而是：

```
Vue dist/
    ↓
Nginx
    ↓
HTTP
```

所以最终前端镜像可以使用：

```
Nginx
+
Vue dist
```

------

## 编译

### .NET 编译

生产环境一般执行：

```
dotnet restore
dotnet publish -c Release -o publish
```

得到：

```
publish/
├── Shop.Api.dll
├── *.dll
├── appsettings.json
└── ...
```

但是在 Docker 中，我们通常不需要开发人员手工执行这些步骤。

而是：

```
Git
 ↓
Docker Build
 ↓
dotnet publish
 ↓
Runtime Image
```

这就是前面讲过的 Multi-stage Build。

------

### Vue 编译

同理：

```
npm install
npm run build
```

可以放入 Docker Build：

```
Dockerfile
    ↓
npm ci
    ↓
npm run build
    ↓
dist
    ↓
Nginx Image
```

最终：

```
开发者
 ↓
git push
 ↓
CI/CD
 ↓
Docker build
```

而不是：

```
开发者
 ↓
手工编译
 ↓
手工复制文件
 ↓
手工上传服务器
```

------

## Docker Image

### .NET API Dockerfile

可以使用：

```
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build

WORKDIR /src

COPY . .

RUN dotnet restore

RUN dotnet publish \
    -c Release \
    -o /app/publish \
    --no-restore


FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime

WORKDIR /app

COPY --from=build /app/publish .

EXPOSE 8080

ENTRYPOINT ["dotnet", "Shop.Api.dll"]
```

构建：

```
docker build \
  -t shop-api:1.0.0 \
  ./backend
```

查看：

```
docker images
```

测试：

```
docker run --rm \
  -p 8080:8080 \
  shop-api:1.0.0
```

然后：

```
curl http://localhost:8080/api/health
```

如果返回：

```
{
  "status": "ok"
}
```

说明：

```
.NET API
 ↓
Docker Image
 ↓
Container
```

已经正常。

------

## Vue + Nginx Dockerfile

前端：

```
FROM node:22 AS build

WORKDIR /app

COPY package*.json ./

RUN npm ci

COPY . .

RUN npm run build


FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html

COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

构建：

```
docker build \
  -t shop-frontend:1.0.0 \
  ./frontend
```

运行：

```
docker run --rm \
  -p 8080:80 \
  shop-frontend:1.0.0
```

浏览器访问：

```
http://localhost:8080
```

此时：

```
Vue
 ↓
npm build
 ↓
dist
 ↓
Nginx
 ↓
Container
```

------

## Registry

本地 Image：

```
shop-api:1.0.0
shop-frontend:1.0.0
```

生产环境需要把它们推送到 Registry。

假设公司 Registry：

```
harbor.example.com
```

那么重新标记：

```
docker tag shop-api:1.0.0 \
  harbor.example.com/shop/shop-api:1.0.0

docker tag shop-frontend:1.0.0 \
  harbor.example.com/shop/shop-frontend:1.0.0
```

登录：

```
docker login harbor.example.com
```

Push：

```
docker push \
  harbor.example.com/shop/shop-api:1.0.0
docker push \
  harbor.example.com/shop/shop-frontend:1.0.0
```

于是：

```
Developer
    │
    │ docker push
    ▼
┌─────────────────────┐
│ Harbor              │
│                     │
│ shop/shop-api       │
│ shop/shop-frontend  │
└──────────┬──────────┘
           │
           │ image pull
           ▼
      Kubernetes
```

------

## Kubernetes Namespace

不要把业务直接部署到 `default` Namespace。

创建：

```
apiVersion: v1
kind: Namespace
metadata:
  name: shop
```

保存：

```
k8s/namespace.yaml
```

执行：

```
kubectl apply -f k8s/namespace.yaml
```

确认：

```
kubectl get namespace shop
```

后面的资源统一：

```
metadata:
  namespace: shop
```

这样业务资源更加容易管理。

------

## 配置文件

这里使用 ConfigMap 保存非敏感配置。

例如：

```
apiVersion: v1
kind: ConfigMap
metadata:
  name: shop-config
  namespace: shop

data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Redis__Host: "redis"
  Redis__Port: "6379"
  Database__Host: "postgresql"
  Database__Port: "5432"
```

创建：

```
kubectl apply -f k8s/configmap.yaml
```

查看：

```
kubectl get configmap -n shop
```

这里体现了一个非常重要的生产原则：

```
Docker Image
    ↓
只包含应用程序

ConfigMap
    ↓
提供环境相关配置
```

因此：

```
同一个 Image
```

可以部署到：

```
Development
Staging
Production
```

只改变配置，而不需要重新构建 Image。

------

## Secret

数据库密码不能放到 ConfigMap。

例如：

```
apiVersion: v1
kind: Secret
metadata:
  name: shop-secret
  namespace: shop

type: Opaque

stringData:
  POSTGRES_USER: "shop"
  POSTGRES_PASSWORD: "change-me"
  REDIS_PASSWORD: "change-me"
```

创建：

```
kubectl apply -f k8s/secret.yaml
```

查看：

```
kubectl get secret -n shop
```

生产环境不要把真实密码直接提交到 Git。

例如不要：

```
Git
└── secret.yaml
    └── production-password
```

更合理的是使用：

```
External Secrets
Vault
云厂商 Secret Manager
```

等方案。

这一点在真正生产环境尤其重要。

------

## PostgreSQL

这里使用 StatefulSet，因为 PostgreSQL 是典型的有状态应用。

一个简化版示例：

```
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: shop

spec:
  selector:
    app: postgresql

  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: shop

spec:
  serviceName: postgresql
  replicas: 1

  selector:
    matchLabels:
      app: postgresql

  template:
    metadata:
      labels:
        app: postgresql

    spec:
      containers:
        - name: postgresql
          image: postgres:16

          ports:
            - containerPort: 5432

          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: shop-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shop-secret
                  key: POSTGRES_PASSWORD

          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data

  volumeClaimTemplates:
    - metadata:
        name: data

      spec:
        accessModes:
          - ReadWriteOnce

        resources:
          requests:
            storage: 10Gi
```

应用：

```
kubectl apply -f k8s/postgresql.yaml
```

检查：

```
kubectl get pod -n shop
```

应该看到类似：

```
postgresql-0
```

查看 PVC：

```
kubectl get pvc -n shop
```

这里再次体现：

```
PostgreSQL
    ↓
StatefulSet
    ↓
PVC
    ↓
PersistentVolume
```

数据库数据不能依赖 Container 本身的临时文件系统。

------

## Redis

Redis 同样可以使用 StatefulSet。

生产环境是否采用单节点 Redis、Redis Sentinel、Redis Cluster 等，需要根据：

```
数据重要性
可用性要求
容量
性能
故障恢复要求
```

进行设计。

学习阶段可以先部署单实例：

```
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: shop

spec:
  selector:
    app: redis

  ports:
    - port: 6379
      targetPort: 6379

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: shop

spec:
  serviceName: redis
  replicas: 1

  selector:
    matchLabels:
      app: redis

  template:
    metadata:
      labels:
        app: redis

    spec:
      containers:
        - name: redis
          image: redis:7

          ports:
            - containerPort: 6379
```

应用：

```
kubectl apply -f k8s/redis.yaml
```

查看：

```
kubectl get pod -n shop
```

此时 Kubernetes 内部已经存在：

```
redis
postgresql
```

两个 Service。

------

## Deployment

现在开始部署 .NET API。

```
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-api
  namespace: shop

spec:
  replicas: 3

  selector:
    matchLabels:
      app: shop-api

  template:
    metadata:
      labels:
        app: shop-api

    spec:
      containers:
        - name: api
          image: harbor.example.com/shop/shop-api:1.0.0

          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: shop-config

          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shop-secret
                  key: POSTGRES_PASSWORD

          readinessProbe:
            httpGet:
              path: /api/health
              port: 8080

            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /api/health
              port: 8080

            initialDelaySeconds: 15
            periodSeconds: 20
```

这里一次使用了前面多个章节的知识：

```
Deployment
    ↓
3 Pods

ConfigMap
    ↓
应用配置

Secret
    ↓
敏感配置

Readiness Probe
    ↓
判断能不能接收流量

Liveness Probe
    ↓
判断应用是否需要重启
```

------

## 私有 Registry 认证

如果 Harbor 是私有 Registry，需要：

```
kubectl create secret docker-registry harbor-secret \
  --namespace shop \
  --docker-server=harbor.example.com \
  --docker-username=YOUR_USERNAME \
  --docker-password='YOUR_PASSWORD'
```

然后 Deployment：

```
spec:
  template:
    spec:
      imagePullSecrets:
        - name: harbor-secret

      containers:
        - name: api
          image: harbor.example.com/shop/shop-api:1.0.0
```

部署：

```
kubectl apply -f k8s/backend.yaml
```

检查：

```
kubectl get deployment -n shop
kubectl get pod -n shop -o wide
```

理想状态：

```
NAME                        READY   STATUS
shop-api-xxxxx              1/1     Running
shop-api-yyyyy              1/1     Running
shop-api-zzzzz              1/1     Running
```

------

## Service

Deployment 解决：

```
Pod 副本管理
```

但 Pod IP 不稳定。

所以需要 Service。

```
apiVersion: v1
kind: Service
metadata:
  name: shop-api
  namespace: shop

spec:
  selector:
    app: shop-api

  ports:
    - port: 80
      targetPort: 8080

  type: ClusterIP
```

应用：

```
kubectl apply -f k8s/backend.yaml
```

查看：

```
kubectl get service -n shop
```

例如：

```
NAME         TYPE        CLUSTER-IP
shop-api     ClusterIP   10.96.100.10
redis        ClusterIP   10.96.100.20
postgresql   ClusterIP   10.96.100.30
```

现在：

```
Pod
10.x.x.x
10.x.x.x
10.x.x.x
```

前面增加了：

```
Service
shop-api
```

因此其他 Pod 不需要知道具体 API Pod IP。

------

## API Service 到 Pod

流量：

```
Frontend
   │
   ▼
shop-api:80
   │
   ▼
Service
   │
   ├── Pod A :8080
   ├── Pod B :8080
   └── Pod C :8080
```

Service 根据：

```
selector:
  app: shop-api
```

找到：

```
app=shop-api
```

的 Pod。

可以检查：

```
kubectl get endpoints -n shop
```

或者：

```
kubectl get endpointslice -n shop
```

如果发现 Service 没有后端 Pod，优先检查：

```
kubectl get pod -n shop --show-labels
```

以及：

```
kubectl describe service shop-api -n shop
```

最常见的问题之一就是：

```
Service selector
        ≠
Pod labels
```

------

## Vue 前端

前端也是无状态应用，因此使用 Deployment。

```
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-frontend
  namespace: shop

spec:
  replicas: 2

  selector:
    matchLabels:
      app: shop-frontend

  template:
    metadata:
      labels:
        app: shop-frontend

    spec:
      containers:
        - name: nginx
          image: harbor.example.com/shop/shop-frontend:1.0.0

          ports:
            - containerPort: 80

          readinessProbe:
            httpGet:
              path: /
              port: 80

          livenessProbe:
            httpGet:
              path: /
              port: 80
```

Service：

```
apiVersion: v1
kind: Service
metadata:
  name: shop-frontend
  namespace: shop

spec:
  selector:
    app: shop-frontend

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP
```

部署：

```
kubectl apply -f k8s/frontend.yaml
```

查看：

```
kubectl get pod -n shop
```

现在：

```
shop-frontend
    ↓
2 Pods
```

------

## Nginx

这里 Nginx 有两个作用。

第一：

```
Vue 静态文件服务器
```

也就是：

```
Browser
 ↓
Nginx
 ↓
index.html
JS
CSS
```

第二，在实际生产环境中，Nginx 还可以处理：

```
静态文件
gzip
缓存
HTTP Header
SPA 路由
```

例如 Vue Router 使用 History 模式时，Nginx 通常需要：

```
server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

否则用户直接访问：

```
https://shop.example.com/order/123
```

可能出现：

```
404
```

因为 Nginx 会认为：

```
/order/123
```

是一个实际文件路径。

------

## Ingress

现在：

```
shop-frontend
shop-api
```

都只有 ClusterIP。

外部用户还不能直接访问。

这时候使用 Ingress。

例如：

```
shop.example.com
```

访问：

```
/
```

进入：

```
shop-frontend
```

而：

```
shop.example.com/api
```

进入：

```
shop-api
```

Ingress：

```
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shop
  namespace: shop

spec:
  ingressClassName: nginx

  rules:
    - host: shop.example.com

      http:
        paths:
          - path: /api
            pathType: Prefix

            backend:
              service:
                name: shop-api
                port:
                  number: 80

          - path: /
            pathType: Prefix

            backend:
              service:
                name: shop-frontend
                port:
                  number: 80
```

应用：

```
kubectl apply -f k8s/ingress.yaml
```

查看：

```
kubectl get ingress -n shop
```

------

## HTTPS

生产环境不能只使用：

```
HTTP
```

应该：

```
HTTPS
```

完整流量：

```
User
  │
  │ HTTPS
  ▼
Ingress
  │
  │ HTTP / Cluster Network
  ▼
Service
  │
  ▼
Pod
```

TLS Certificate 通常保存为：

```
TLS Secret
```

例如：

```
kubectl create secret tls shop-tls \
  --namespace shop \
  --cert=tls.crt \
  --key=tls.key
```

查看：

```
kubectl get secret shop-tls -n shop
```

Ingress：

```
spec:
  tls:
    - hosts:
        - shop.example.com
      secretName: shop-tls

  rules:
    - host: shop.example.com
      http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: shop-frontend
                port:
                  number: 80
```

于是：

```
https://shop.example.com
```

就可以通过 TLS 建立 HTTPS。

------

## HTTP → HTTPS

生产环境通常不希望：

```
http://shop.example.com
```

继续提供明文访问。

目标应该是：

```
HTTP
 ↓
301/308
 ↓
HTTPS
```

具体实现取决于使用的 Ingress Controller。

如果使用 Nginx Ingress，通常可以通过 Ingress Controller 的 HTTPS Redirect 能力实现。

最终用户体验：

```
http://shop.example.com
          │
          ▼
https://shop.example.com
```

------

## 完整流量链路

现在把整个系统连起来。

用户访问：

```
https://shop.example.com
```

首先：

```
DNS
 ↓
Ingress Controller
```

Ingress 根据：

```
Host = shop.example.com
Path = /
```

路由到：

```
shop-frontend Service
```

Service：

```
shop-frontend
      ↓
┌─────┴─────┐
▼           ▼
Pod A       Pod B
Nginx       Nginx
```

Nginx 返回：

```
index.html
JavaScript
CSS
```

Vue 开始运行。

如果 Vue 调用：

```
https://shop.example.com/api/hello
```

则：

```
Browser
   │
   ▼
Ingress
   │
   │ /api
   ▼
shop-api Service
   │
   ├── API Pod A
   ├── API Pod B
   └── API Pod C
```

API 需要查询数据库：

```
API Pod
   │
   ├──────────────► redis:6379
   │
   └──────────────► postgresql:5432
```

最终：

```
                         Internet
                            │
                         HTTPS
                            │
                            ▼
                    ┌──────────────┐
                    │    Ingress   │
                    └──────┬───────┘
                           │
                ┌──────────┴──────────┐
                │                     │
              /api                    /
                │                     │
                ▼                     ▼
        ┌─────────────┐       ┌─────────────┐
        │ shop-api    │       │ shop-frontend│
        │ Service     │       │ Service      │
        └──────┬──────┘       └──────┬──────┘
               │                     │
        ┌──────┼──────┐         ┌────┴────┐
        ▼      ▼      ▼         ▼         ▼
       API    API    API       Nginx     Nginx
        │
        ├──────────► Redis
        │
        └──────────► PostgreSQL
```

这就是一个完整的 Kubernetes 业务系统。

------

## 应用配置如何进入 .NET

前面定义了：

```
ConfigMap:
  Redis__Host: redis
  Database__Host: postgresql
```

.NET Configuration 默认支持环境变量配置层级。

例如：

```
Redis__Host
```

对应：

```
Redis:Host
```

因此代码可以：

```
var redisHost =
    builder.Configuration["Redis:Host"];
```

数据库：

```
var databaseHost =
    builder.Configuration["Database:Host"];
```

这样就实现：

```
Kubernetes ConfigMap
        ↓
Environment Variable
        ↓
.NET Configuration
        ↓
Application
```

------

## Secret 如何进入 .NET

例如：

```
env:
  - name: POSTGRES_PASSWORD
    valueFrom:
      secretKeyRef:
        name: shop-secret
        key: POSTGRES_PASSWORD
```

Container 内：

```
POSTGRES_PASSWORD=******
```

.NET：

```
var password =
    builder.Configuration["POSTGRES_PASSWORD"];
```

这样：

```
Secret
 ↓
Environment Variable
 ↓
.NET Application
```

而密码不需要写入 Docker Image。

------

## PostgreSQL 连接字符串

生产环境常见的做法是把数据库配置拆开。

例如：

```
Database__Host=postgresql
Database__Port=5432
Database__Database=shop
Database__User=shop
Database__Password=******
```

.NET：

```
var connectionString =
    $"Host={configuration["Database:Host"]};" +
    $"Port={configuration["Database:Port"]};" +
    $"Database={configuration["Database:Database"]};" +
    $"Username={configuration["Database:User"]};" +
    $"Password={configuration["Database:Password"]}";
```

更推荐在实际项目中统一使用标准的 Connection String 配置方式，而不是让业务代码到处拼接。

核心思想不变：

```
应用代码
    ↓
Configuration
    ↓
Kubernetes ConfigMap / Secret
```

------

## Redis 连接

Kubernetes Service 提供稳定 DNS：

```
redis
```

因此 API 不需要：

```
redis Pod IP
```

例如：

```
10.244.1.25
```

而只需要：

```
redis:6379
```

因为：

```
redis
 ↓
Kubernetes DNS
 ↓
Redis Service
 ↓
Redis Pod
```

同理 PostgreSQL：

```
postgresql:5432
```

这就是 Kubernetes Service Discovery 的实际应用。

------

## 完整部署顺序

整个系统第一次部署时，可以按照依赖关系：

```
Namespace
    ↓
ConfigMap / Secret
    ↓
PostgreSQL
    ↓
Redis
    ↓
Backend Deployment
    ↓
Backend Service
    ↓
Frontend Deployment
    ↓
Frontend Service
    ↓
Ingress
    ↓
DNS
    ↓
HTTPS
```

实际执行：

```
kubectl apply -f k8s/namespace.yaml

kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secret.yaml

kubectl apply -f k8s/postgresql.yaml
kubectl apply -f k8s/redis.yaml

kubectl apply -f k8s/backend.yaml
kubectl apply -f k8s/frontend.yaml

kubectl apply -f k8s/ingress.yaml
```

------

## 部署后的检查

不要执行完：

```
kubectl apply
```

就认为系统部署成功。

生产环境必须验证每一层。

------

### 检查 Namespace

```
kubectl get all -n shop
```

------

### 检查 Pod

```
kubectl get pod -n shop -o wide
```

目标：

```
STATUS = Running
READY  = 1/1
```

------

### 检查 Deployment

```
kubectl get deployment -n shop
```

例如：

```
NAME             READY   UP-TO-DATE   AVAILABLE
shop-api         3/3     3            3
shop-frontend    2/2     2            2
```

------

### 检查 StatefulSet

```
kubectl get statefulset -n shop
```

例如：

```
NAME          READY
redis         1/1
postgresql    1/1
```

------

### 检查 PVC

```
kubectl get pvc -n shop
```

目标：

```
STATUS = Bound
```

如果：

```
Pending
```

说明存储没有正常提供。

------

### 检查 Service

```
kubectl get service -n shop
```

确认：

```
shop-api
shop-frontend
redis
postgresql
```

都存在。

------

### 检查 EndpointSlice

```
kubectl get endpointslice -n shop
```

重点确认：

```
shop-api
shop-frontend
redis
postgresql
```

是否存在正确的后端地址。

如果 Service 存在，但是没有 Endpoint，通常说明：

```
Selector
```

和：

```
Pod Label
```

没有正确匹配，或者 Pod 没有达到 Ready 状态。

------

### 检查 Ingress

```
kubectl get ingress -n shop
```

查看详细信息：

```
kubectl describe ingress shop -n shop
```

------

## DNS

Ingress 配置：

```
shop.example.com
```

之后 DNS 必须解析到 Ingress 的入口地址。

例如：

```
shop.example.com
       ↓
     DNS
       ↓
203.0.113.10
       ↓
Ingress Controller
```

验证：

```
nslookup shop.example.com
```

或者：

```
dig shop.example.com
```

注意：

> Kubernetes Ingress 创建成功，不代表公网 DNS 已经配置完成。

这是两个不同的系统。

------

## HTTPS 验证

```
curl -I https://shop.example.com
```

正常情况下应该看到：

```
HTTP/2 200
```

或者：

```
HTTP/1.1 200
```

检查证书：

```
openssl s_client \
  -connect shop.example.com:443 \
  -servername shop.example.com
```

重点检查：

```
Certificate
Subject
Issuer
Validity
```

以及域名是否匹配。

------

## API 验证

```
curl https://shop.example.com/api/health
```

应该返回：

```
{
  "status": "ok"
}
```

这意味着完整链路已经打通：

```
用户
 ↓
DNS
 ↓
Ingress
 ↓
Service
 ↓
API Pod
 ↓
.NET
```

------

## API → Redis / PostgreSQL 验证

如果 API 返回数据库查询结果，链路进一步变成：

```
User
 ↓
Ingress
 ↓
API Service
 ↓
API Pod
 ├── Redis Service
 │      ↓
 │    Redis
 │
 └── PostgreSQL Service
        ↓
     PostgreSQL
```

可以进入 API Pod：

```
kubectl exec -it \
  deploy/shop-api \
  -n shop \
  -- /bin/sh
```

测试 DNS：

```
getent hosts redis
getent hosts postgresql
```

如果能够解析：

```
redis
postgresql
```

说明 Kubernetes Service DNS 工作正常。

------

## 一个非常重要的生产区别：前端调用 API 的地址

很多初学者会写：

```
http://shop-api:80/api
```

然后让浏览器访问。

这是错误的。

因为：

```
shop-api
```

是 Kubernetes 集群内部 DNS。

浏览器运行在：

```
用户电脑
```

并不在 Kubernetes Cluster 内。

所以：

```
Browser
    X
    │
    └── shop-api:80
```

无法直接访问。

正确方式是：

```
Browser
   │
   ▼
https://shop.example.com/api
   │
   ▼
Ingress
   │
   ▼
shop-api Service
```

因此前端可以使用：

```
/api
```

作为 API Base URL。

例如：

```
const apiBaseUrl = "/api";
```

这样非常适合：

```
https://shop.example.com
```

和：

```
https://shop.example.com/api
```

统一入口的架构。

------

## 生产环境中的镜像版本

假设第一次发布：

```
shop-api:1.0.0
shop-frontend:1.0.0
```

第二次：

```
shop-api:1.0.1
shop-frontend:1.0.1
```

Deployment 修改：

```
image: harbor.example.com/shop/shop-api:1.0.1
```

然后：

```
kubectl apply -f k8s/backend.yaml
```

Deployment 会执行 Rolling Update。

查看：

```
kubectl rollout status \
  deployment/shop-api \
  -n shop
```

查看历史：

```
kubectl rollout history \
  deployment/shop-api \
  -n shop
```

如果新版本有问题：

```
kubectl rollout undo \
  deployment/shop-api \
  -n shop
```

这就是：

```
Image Version
     ↓
Deployment
     ↓
Rolling Update
     ↓
Rollback
```

------

## 配置、代码、Secret 的职责分离

整个系统应该形成这样的边界：

```
┌──────────────────────────────┐
│ Docker Image                │
│                              │
│ Application Code             │
│ Runtime                      │
│ Dependencies                │
└──────────────┬───────────────┘
               │
               ▼
       ┌───────────────┐
       │ Kubernetes    │
       │               │
       │ ConfigMap     │─── 普通配置
       │ Secret        │─── 敏感配置
       └───────────────┘
```

也就是说：

**代码进入 Image。**

**环境配置进入 ConfigMap。**

**密码、Token 等敏感数据进入 Secret。**

**数据库数据进入 PVC。**

这四者不要混在一起。

------

## 最终生产架构

把这一章所有内容组合起来：

```
                         ┌───────────────┐
                         │   Developer   │
                         └───────┬───────┘
                                 │
                              Git Push
                                 │
                                 ▼
                         ┌───────────────┐
                         │    CI/CD      │
                         │ Build / Test  │
                         └───────┬───────┘
                                 │
                         Docker Build
                                 │
                  ┌──────────────┴──────────────┐
                  ▼                             ▼
          shop-api:1.0.0                shop-frontend:1.0.0
                  │                             │
                  └──────────────┬──────────────┘
                                 │
                              Push
                                 ▼
                     ┌─────────────────────┐
                     │       Harbor        │
                     └──────────┬──────────┘
                                │
                             Pull Image
                                │
                                ▼
                    ┌───────────────────────┐
                    │    Kubernetes        │
                    │                       │
                    │  Deployment           │
                    │     │                 │
                    │     ▼                 │
                    │   API Pods            │
                    │                       │
                    │  Deployment           │
                    │     │                 │
                    │     ▼                 │
                    │ Frontend Pods          │
                    │                       │
                    │ StatefulSet             │
                    │   ├── Redis             │
                    │   └── PostgreSQL        │
                    └───────────┬────────────┘
                                │
                              Service
                                │
                                ▼
                             Ingress
                                │
                              HTTPS
                                │
                                ▼
                              User
```

配置：

```
ConfigMap
    ├── Redis Host
    ├── PostgreSQL Host
    └── Application Settings

Secret
    ├── Database Password
    ├── Redis Password
    └── Other Credentials

TLS Secret
    └── HTTPS Certificate
```

数据：

```
PostgreSQL
    ↓
PVC
    ↓
Persistent Storage
```

------

## 从代码到用户访问的完整链路

最终可以把整个 Kubernetes 发布过程浓缩成：

```
① 开发
   ↓
Git Repository

② 编译
   ↓
.NET publish / Vue build

③ 构建镜像
   ↓
Docker Image

④ 镜像安全扫描 / 测试
   ↓
Registry

⑤ Kubernetes Deployment
   ↓
Pod

⑥ Service
   ↓
稳定的集群内部访问

⑦ Ingress
   ↓
域名 / Path 路由

⑧ TLS
   ↓
HTTPS

⑨ DNS
   ↓
用户

⑩ 用户访问
```

而一次真正的 HTTP 请求：

```
https://shop.example.com/api/hello
             │
             ▼
            DNS
             │
             ▼
      Ingress Controller
             │
       /api 路由
             │
             ▼
      shop-api Service
             │
      ┌──────┼──────┐
      ▼      ▼      ▼
     Pod    Pod    Pod
      │
      ▼
   .NET API
      │
 ┌────┴─────┐
 ▼          ▼
Redis    PostgreSQL
             │
             ▼
            PVC
```

至此，就真正把前面学习的核心 Kubernetes 知识连接成了一个完整系统：

```
Pod
Deployment
StatefulSet
Service
Ingress
ConfigMap
Secret
PVC
Image
Registry
Nginx
HTTPS
Health Check
DNS
```

这才是 Kubernetes 在实际生产环境中的基本工作方式：**不是单独运行某一个 Pod，而是把应用、网络、配置、密钥、存储和发布流程组合成一个可持续运行、可更新、可回滚的完整系统。**

